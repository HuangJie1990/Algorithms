package ChapterTwo;

import edu.princeton.cs.algs4.In;
import edu.princeton.cs.algs4.StdRandom;

public class MaxPQTest {

    public static void main(String[] args) {
/*        String[] a = new In(args[0]).readAllStrings();
        StdRandom.shuffle(a);
        for (String s : a) {
            System.out.print(s + " ");
        }
        System.out.println();
        HeapMaxPQ<String> pq = new HeapMaxPQ<>(50);
        for (String s : a) {
            pq.insert(s);
        }
        for (String s : pq) {
            System.out.print(s + " ");
        }
        System.out.println(String.format("pq's size is %d, pq's capacity is %d", pq.size(), pq.capacity()));
        System.out.println(pq.max());
        for (String s : pq) {
            System.out.print(s + " ");
        }
        System.out.println(String.format("pq's size is %d, pq's capacity is %d", pq.size(), pq.capacity()));
        System.out.println(pq.delMax());
        for (String s : pq) {
            System.out.print(s + " ");
        }
        System.out.println(String.format("pq's size is %d, pq's capacity is %d", pq.size(), pq.capacity()));
        System.out.println(pq.delMax());
        System.out.println(pq.delMax());System.out.println(pq.delMax());
        System.out.println(pq.delMax());
        System.out.println(pq.delMax());
        System.out.println(pq.delMax());

        for (String s : pq) {
            System.out.print(s + " ");
        }
        System.out.println(String.format("pq's size is %d, pq's capacity is %d", pq.size(), pq.capacity()));*/
        String[] a = new In(args[0]).readAllStrings();
        StdRandom.shuffle(a);
        for (String s : a) {
            System.out.print(s+" ");
        }
    }
}
